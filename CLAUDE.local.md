<!--
  ЩО ЦЕ: CLAUDE.local.md — особисті нотатки/інструкції ДЛЯ ТЕБЕ.
  У РЕАЛЬНОМУ проєкті цей файл має бути в .gitignore (не для команди).

  ⚠️ У ЦЬОМУ ДЕМО-ПРОЄКТІ .gitignore навмисно закоментований для нього,
  щоб файл був видимий на GitHub як приклад того, як він виглядає.
  Не забудь розкоментувати правило в .gitignore, якщо форкаєш цей
  проєкт для реальної роботи.

  ЯК СТВОРИТИ: просто створи файл вручну в корені проєкту з такою назвою.
  Claude Code підхоплює його автоматично поряд з CLAUDE.md.

  КОЛИ ВИКОРИСТОВУВАТИ: коли працюєш у власній гілці над рефакторингом
  і хочеш, щоб Claude тримав у голові архітектурні рішення, які НЕ мають
  впливати на решту команди (бо це особисте, ще не узгоджене рішення).
-->

# Personal Notes (only for me, this repo only)

- Working on migrating `src/api/handlers` to server actions — in progress,
  don't assume this migration is finished elsewhere in the codebase.
- Prefer verbose explanations when Claude proposes DB schema changes —
  I want to understand the "why", not just the "what".
