#!/usr/bin/env bash
#
# ЩО ЦЕ: приклад допоміжного скрипта в scripts/ — типовий use case:
# "environment validation". Claude запускає, не читає вміст.

echo "=== (демо) Validating environment ==="
echo "→ у реальному проєкті тут перевірка: node --version, npm ls, .env наявність тощо"
echo "OK"
